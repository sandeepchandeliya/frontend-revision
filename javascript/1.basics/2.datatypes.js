// Numbers
//  2, -45.

// STRINGS
// ' ' , " ", `${}`. can use single quotes double quotes and template literals using backticks
const hero = 'Batman!';
console.log(`${hero} is so cool!`);

let errorMessage = 'an error \n' + 'occurred';

console.log(errorMessage);
