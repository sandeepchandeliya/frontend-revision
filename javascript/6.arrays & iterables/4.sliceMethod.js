const gamesList = ['black myth', 'spiderman 2', 'Gta 6', 'metal gear'];


// SELECTING RANGES AND CREATING COPIES WITH SLICE

console.log(gamesList.slice(0,2));

// ARRAYS WITH CONCAT
const concatedArr = gamesList.concat(["cuphead"]);
console.log(concatedArr);
