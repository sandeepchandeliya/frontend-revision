for creating tsconfig file --> tsc --init

--> React Components → interfaces for props, ReactNode, optional props.
--> State → generics (useState<T>), unions.
--> Events → ChangeEvent, MouseEvent, FormEvent.
--> Fetching Data → Promise<T>, async/await, typed API responses.
--> Context & Reducers → discriminated unions, interfaces, utility types.
--> Custom Hooks → generics.
